export enum OrderStatus {
  SCHEDULED = 'scheduled',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}

export enum OrderStatusColour {
  scheduled = 'orange',
  completed = 'green',
  cancelled = 'red',
}
